# æ­¤å·¥ä½œæ?ç¨‹æ?æ§‹å»º?Œéƒ¨ç½?Node.js ?‰ç”¨ç¨‹å???Azure Web App
# ?´å?ä¿¡æ¯è«‹å??? https://docs.microsoft.com/azure/developer/javascript/tutorial-vscode-azure-app-service-node-01

name: æ§‹å»º?Œéƒ¨ç½?Node.js ?‰ç”¨ç¨‹å???Azure Web App

on:
  push:
    branches: [ "master", "main" ]
  workflow_dispatch:

env:
  AZURE_WEBAPP_NAME: orange-trade    # è¨­ç½®?ºæ‚¨?„æ??¨ç?å¼å?ç¨?
  AZURE_WEBAPP_PACKAGE_PATH: '.'         # è¨­ç½®?ºé??®ç?è·¯å?ï¼Œé?è¨­ç‚º?¹ç›®??
  NODE_VERSION: '18.x'                   # è¨­ç½®?ºè?ä½¿ç”¨??Node.js ?ˆæœ¬

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4

    - name: è¨­ç½® Node.js
      uses: actions/setup-node@v4
      with:
        node-version: ${{ env.NODE_VERSION }}
        cache: 'npm'

    - name: å®‰è?ä¾è³´?æ?å»ºå?æ¸¬è©¦
      run: |
        npm ci
        npm run build --if-present
        # npm run test --if-present

    - name: ?“å??‰ç”¨ç¨‹å?ä»¥é€²è??¨ç½²
      run: zip -r node-app.zip . -x "*.git*" "node_modules/*" "tests/*" "*.md"

    - name: ä¸Šå‚³å»ºæ??©ä»¶ä»¥ä??¨ç½²ä½œæ¥­ä½¿ç”¨
      uses: actions/upload-artifact@v4
      with:
        name: node-app
        path: node-app.zip

  deploy:
    permissions:
      contents: none
    runs-on: ubuntu-latest
    needs: build
    environment:
      name: 'Production'
      url: ${{ steps.deploy-to-webapp.outputs.webapp-url }}

    steps:
    - name: ä¸‹è?å»ºæ??©ä»¶
      uses: actions/download-artifact@v4
      with:
        name: node-app

    - name: è§??å»ºæ??©ä»¶
      run: unzip node-app.zip

    - name: '?¨ç½²??Azure Web App'
      id: deploy-to-webapp
      uses: azure/webapps-deploy@v2
      with:
        app-name: ${{ env.AZURE_WEBAPP_NAME }}
        publish-profile: ${{ secrets.AZURE_WEBAPP_PUBLISH_PROFILE }}
        package: ${{ env.AZURE_WEBAPP_PACKAGE_PATH }} 
